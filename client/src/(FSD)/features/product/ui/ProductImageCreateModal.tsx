"use client";

import { ModalProps } from "@nextui-org/modal";
import React from "react";

interface ProductImageCreateModalProps extends ModalProps {
    
}

const ProductImageCreateModal = ({  } : ProductImageCreateModalProps) => {
    return (
        <div>

        </div>
    )
}

export default ProductImageCreateModal;